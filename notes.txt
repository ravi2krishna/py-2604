20th Feb 2026
=============

    -> Python Course - 12 PM IST 

        -> Full Stack Development: SQL + HTML / JS + Django etc 

            -> Build Web Applications 

        -> Data Analytics / Data Science / AI Agents: Numpy / Pandas / PyTorch etc 

            -> Build Visualizations, Build Models. Build Agents etc 

        -> Cloud / DevOps : OS + Scripting + Boto3 ect 

            -> Build Automation Scripts (Build Infrastructure ) 

        -> Testing: Automation Testing 

            -> Selenium Automation Scripts (Python)


    -> Course Name: Core & Advance Python, which is common for all above courses 

        -> Python Language Fundamentals 



    -> Python

        -> Python is a high-level, general-purpose programming language. 
        Its design philosophy emphasizes code readability with the 
        use of significant indentation. 
        Python is dynamically type-checked and garbage-collected.


        -> Python is a high-level programming language

            -> High Level Language means "Human Understandable Language" (English)

            -> Python Code Looks Like English 

                num1 = 1.5
                num2 = 6.3
                sum = num1 + num2

        -> Low Level language

            -> Low Level Language means "Machine Understandable Language" (010101010)

            -> 01110000 01111001 01110100 01101000 01101111 01101110 -> python 

        
        
        -> Python is a general-purpose programming language

            -> If we can build multiple types of Applications 

                -> Desktop Applications ? -> Yes 

                -> Web Applications ? -> Yes 

                -> API's ? -> Yes 

                -> Automation / Testing Scripts ? -> Yes 

        -> Domain Specific programming language

            -> Structured Query Language is a domain-specific language 
            used to manage data, especially in a relational database management system(RDBMS)

                -> Desktop Applications ? -> NO 

                -> Web Applications ? -> NO 

                -> API's ? -> NO

    -> Python design philosophy emphasizes "code readability" with the 
        use of significant "indentation"

        -> Code readability

            Java Code
            ---------

                class Main {

                    public static void main(String[] args) {
                        
                        int first = 10;
                        int second = 20;

                        // add two numbers
                        int sum = first + second;
                        System.out.println(first + " + " + second + " = "  + sum);
                    }
                }

            class
            public
            static
            void
            main
            []
            ()
            {}
            ;

            Python Code
            -----------

                num1 = 1.5
                num2 = 6.3
                sum = num1 + num2
                print(sum)

        -> Indentation

            -> Indentation refers to Code Structuring 

            -> In Python we use "spaces" for indentation

            -> ABC Language - https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTele9NP843yKTjtsSbAFoDgEUvS28n14OcgBXzPl4v0pBF49910b1s5B8XZS1kssqRQsrIC8KZMRWATvOoxR2PY7hvCCxQnJH6LkT0lw&s=10

            
    -> Python is dynamically type-checked and garbage-collected.

        -> Dynamically type-checked

            -> We have a concept called "Data Types" 

                -> Data Types: Categorization Of Data

                    -> text, whole numbers, decimal numbers etc 

            -> NOTE: Dynamically type-checked means, we don't need to explicitly 
            specify Data Types 

                -> product = "iphone 17"
                -> price = 120000 
                -> rating = 4.6
                -> etc 


        -> Static type-checked

             -> NOTE: Static type-checked means, we must explicitly 
            specify Data Types 

                -> String product = "iphone 17";
                -> int price = 120000;
                -> double rating = 4.6;
                -> etc 


    -> Python is Garbage-collected 

        -> Automatically remove "Unused" Data 

            -> Python Automatically cleans up unused data from memory 

                product = "iphone 17"
                price = 120000 
                rating = 4.6
                quantity = 25

                print(product)
                print(price)
                print(rating)

            -> NOTE: Python cleans up quantity memory, as it's never used in program


    -> Python is an "Interpreted" Language 

        -> An Interpreted Language executes line by line 

    -> Java is "Compiled" Language

        -> A Compiled Language translates all the code into machine code and executes 
        all the code at Once 

        -> Compiled Language are FASTER compared to Interpreted Language

    -> NOTE: Performance is impacted Java > Python 

    -> Consider Python / StillWhy Python 

        -> Easy To Understand / Easy Readability / Dynamically Type checked

        -> Development SPEED 

        -> Python is BEST PROTO TYPE Language, 



23rd Feb 2026
=============

    -> Programming Paradigms 

        -> A programming paradigm is a relatively high-level way to conceptualize 
        and structure the implementation of a computer program.

        -> Problem: How To Reach Institute ? 

            -> Solution 1: I'll come by Walk 

            -> Solution 2: I'll come by Bike 

            -> Solution 3: I'll come by Car 

            -> etc 
        

        -> We have Two Popular Types

            -> Imperative Approach 

                -> Focus in on "HOW TO DO"

                -> Object Oriented Programming (OOP)


            -> Declarative Approach 

                -> Focus in on "WHAT TO DO"

                -> Functional Programming 

    -> NOTE: Python uses multi paradigm approach i.e we use 
    Imperative Approach & Declarative Approach 


    -> Imperative Code With Python 

                def get_user_emails(users):
                        emails = []  # Step-by-step building the result

                        for i in range(len(users)):         # Loop manually
                            user = users[i]                 # Fetch each user

                            if user.get("email") is not None:   # Check condition manually
                                emails.append(user["email"])    # Manually add to list

                        return emails


                    # Sample users list
                    users = [
                        {"name": "Mike", "email": "mike@gmail.com"},
                        {"name": "John", "email": None},
                        {"name": "Sara", "email": "sara@gmail.com"}
                    ]

                    print(get_user_emails(users))


    -> Declarative Code With Python 

            emails = [u["email"] for u in users if u["email"] is not None]

    
    -> Full Stack Developer: Object Oriented Programming (Imperative)

    -> Data / DevOps / Cloud: Functional Programming (Declarative)


    -> Key Features Of Python / Why Python ?

        -> Simplicity & Easy Readability 

        -> Versatility 

            -> Data Oriented / Full Stack / Cloud / DevOps etc 
        
        -> Multi Paradigm Programming Language (OOP / Functional)

        -> Dynamic Typed 

        -> Open Source (FREE)

        -> High Level Language (Just Like English)

        -> One of the Most Popular Language in industry now 

            -> List Of Popular Applications Where Python is used 

            -> https://en.wikipedia.org/wiki/Programming_languages_used_in_most_popular_websites  

            -> Python is for Backend (Business Logic Building)

    
    -> System Requirements For Python 

        -> Hardware 

            -> i3 / i5 Processor 

            -> 4 GB RAM / 8 GB RAM 

            -> 500 GB HDD / SSD 

        -> Softwares 

            -> Install Python Interpreter 

                -> https://www.python.org/

            
            -> IDE: Integrated Development Environment

                -> Tools for writing code (syntax highlight, auto code completion, extensions etc)

                -> Visual Studio Code - VS Code 

    -> Python Setup 

        -> open command prompt (windows)

            -> python --version 

                -> if not found download exe and install 

    -> Where To Write Code ? 

        -> Command Line Mode (CLI) (Not Recommended)

            -> We write the code in command prompt(win) / terminal(mac)

            -> "Temporary", once we close the session all code is reset 

            -> Enter into command line mode using python

        
        -> Script Mode (Recommended)

            -> We write the code in files using IDE i.e Visual Studio Code 

            -> "Permanent", stored on disk 

    
    -> How To Write Code ? 

        -> Any Programming Language has Rules i.e "Syntax"

        -> Comparing programming language with english language

            -> i taking class now today at 12:00 PM now (incorrect)

            -> i'm taking class today at 12:00 PM (correct)

        
        -> Learn English

            -> English has 26 Alphabets (abcd.......z)

            -> Using these Alphabets, we learn how to form words and complex sentences over time


        -> Learn Python

            -> Python has 35 Keywords 

            -> Using these Keywords we form statements, using correct syntax over time  

            -> List Of Python Keywords

                -> https://docs.python.org/3/reference/lexical_analysis.html#keywords

                    False      await      else       import     pass
                    None       break      except     in         raise
                    True       class      finally    is         return
                    and        continue   for        lambda     try
                    as         def        from       nonlocal   while
                    assert     del        global     not        with
                    async      elif       if         or         yield

                -> NOTE: All the above keywords hold some special meaning to python Interpreter 


24th Feb 2026
=============

    -> Python Rules / Syntax / Best Practices 

        -> Any Python File we write, should have extension as .py 

        -> Python ignores lines that start with # 

        -> Comments starts with # 

        -> In python when working with numeric data(1,2,0,-1,5.5)
        we can directly use print() with numerics 

        -> In python when working with text data, we call it as "String"

            -> In Python Strings are represented using '' or ""


    -> Identifiers 

        -> An identifier is a "name" used to uniquely identify a piece of data 
        in python code 

            -> Variable 
            -> Function 
            -> Class 
            -> Object 
            -> etc 

    -> Identifiers Rules 

            -> Can Contain

                -> Letters (a-z A-Z)
                -> Digits (0-9)
                -> Special Symbols ( _ Only underscore)

            -> Must Start With 

                -> A letter or _ (underscore)

    -> NOTE: Always use proper way of naming identifiers        

    
    
    -> Variables 

        -> Variable is a named storage location in computers 
        memory (RAM), that holds value or data 

            -> Computer -> RAM -> Variables  

        -> Variable Syntax 

            -> variable_name = value 

                -> variable_name should follow identifier Rules
                -> = assignment operator 
                -> value is data you want to store 

                    -> data = 10 (valid)
                    -> $data= 10 (invalid)
                    -> data = ten (invalid)
                    -> data = 'ten' (valid)

        -> Variables Allow You To 

            -> Store Data (Assign)

            -> Retrieve Data (Get)

            -> Manipulate Data (Change)  

        -> Combine Variables with our custom statements 

            -> Concatenation: Joining Text Data(Strings) using + operator

            -> When Combining Strings with Other Data types Like
            int, float, boolean etc we use , operator

        -> To Know what type of data / value we are using

            -> use type() function to know what data type is 


25th Feb 2026
=============

    -> You can check the memory address assigned to data using 

        -> id() function : gives memory address 

    -> Memory Model In Python 

        -> If a value is already present in memory, 
        python will not create a new memory block for same value 

    
    -> Manipulate Data (Change)  

        -> Using "Operators" 

    -> Operators

        -> Operators are special "Symbols" and "keywords", which Performs 
        Operations on Variables and their Values 

        -> There are different types of operators, to deal with different types of data 

        -> Arithmetic Operators

            -> To Perform Mathematical Calculations 

            -> + - * / % 
            -> // - floor division (same as division but rounds off result)
            -> ** - Exponentiation (used to Calculate powers)

        -> Task Calculate EMI 

            -> https://www.cardekho.com/mahindra/xev-9e

            -> https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQK33TygWLqqVu8nVVISPtiyO3AyplZm3rheg&s


        -> Compound Assignment Operators  

            -> Assignment Operator is = (used for storing a value)

            -> Compound Assignment Operators  Combine 
            Arithmetic Operators with Assignment Operator
            for "short hand code" (concise code)

                -> x = 10 
                -> x = x + 5 (long form)
                -> x += 5 (short form)

                -> x = x * 5 (long form)
                -> x *= 5 (short form)

            -> In Other Programming Languages we got 
            Increment & Decrement Operators 

                -> Increment (++)
                -> Decrement (--)

            -> NOTE: Above Syntax is invalid in python 


        -> Comparison Operators

            -> Comparison Operators are used for Comparing Values
            and result is in boolean type (True/False)  

            -> Especially used in Conditional Checks 

            -> ==, !=, >, <, >=, <=

        
        -> Logical Operators

            -> For Performing Logical Operations 

            -> Especially used in Multiple Conditional Checks 
            and result is in boolean type (True/False)  

            -> and: if both conditions are True, then only result is True, Otherwise False
            -> or: if one of the conditions is True, then only result is True, Otherwise False
            -> not: negates the result, if True then False and Vice versa

                            X       Y       X and Y    X or Y    not X    not Y
                            T       T           T         T         F       F
                            T       F           F         T         F       T
                            F       T           F         T         T       F
                            F       F           F         F         T       T 


        -> Membership Operators

            -> Used for checking if an object is in given sequence 

                -> in: Returns True, if an object is in given sequence 

                -> not in: Returns True, if an object is not in given sequence 

                -> Especially used in Data Structure like Lists, Tuples etc 

                    -> NOTE: in and not in are used with Iterables 

                        -> Iterables

                            -> An iterable is any Python object that can be looped over, 
                            meaning it can return its elements one at a time. 
                            Common built-in iterables include: 
                                -> Lists
                                -> Tuples
                                -> Strings
                                -> Dictionaries
                                -> Sets
                                -> etc  

                            -> NOTE: Technically, an object is considered iterable 
                            if it implements a special method called __iter__()

                            -> We have dir() - tells what kind of Operations
                            we can Perform on a data type 


26th Feb 2026
=============

    -> Identity Operators

        -> Used To Check if objects are same 

            -> is: Returns True, if objects are same 

            -> is not: Returns True, if objects are not same 

        -> NOTE: 

            ->  (Equality operator ==): Compares the values of two objects. 
                It returns True if their contents are the same, 
                regardless of where they are stored in memory.

            -> is (Identity operator): Compares the identity (memory address) of two objects. 
                It returns True only if both variables refer to the exact same object in memory.    


    -> Bitwise Operator 

        -> Used in Low Level Programming (0101010101)

            -> & - if both the bits are 1, then result is 1 
            -> | - if one of the bits is 1, then reset is 1 
            -> etc 


    -> Data Types 

        -> Data Types are Categorization that defines, 
        what kind of data a Variable can hold 

    -> Numeric Types 

        -> int: 1,2,3,0,-1,-2 etc 

        -> float: 1.0,2.0,3.5,0.0,-1.0,-2.5 etc 

        -> complex:

            -> a + ib (math)

            -> a + bj 

    -> Boolean Type

        -> bool: Represents True / False (case sensitive)

    -> None Type

        -> NoneType: Represents Absence Of Value 

    -> Strings 

        -> str: String is sequence of characters enclosed in '' or ""

    -> NOTE:

        -> Simple Data Types

            -> Holds Single Value in a Single Variable 

        -> Complex Data Types 

            -> Holds Multiple Values in a Single Variable

    -> List: List is a complex Datatype i.e holds multiple values
    and is represented using [] 

    -> Tuple: Tuple is a complex Datatype i.e holds multiple values
    and is represented using () 

    -> Set: Set is a complex Datatype i.e holds multiple values
    and is represented using {} 

    -> Frozenset: Frozenset is a complex Datatype i.e holds multiple values
    and is represented using frozenset class  

    -> Dictionary: Dictionary is a complex Datatype i.e holds 
    multiple key and values and is represented using {} 

    -> NOTE: All the above Data types are predefined in python 

    -> NOTE: Based On Custom Requirements coming from clients,
    we can create our own data types i.e User Defined Datatype 

    -> Create Custom Datatype

        -> syntax

            class CustomDataType:
                statements


27th Feb 2026
=============

    -> Type Conversion / Implicit Conversion [Automatic]

        -> Converting One Data type to another data type, 
        Automatically by Python 

        -> Type Conversion is SAFE i.e no DATA LOSS

    
    -> Type Casting / Explicit Conversion [ Manual ]

        -> Converting One Data type to another data type, Manually by Developer 

        -> Type Casting is NOT SAFE i.e DATA LOSS might occur 

        -> syntax

            -> data_type(value) # casting functions 


    -> Control Statements / Control Structures 

        -> Control Structures helps you determine the "flow of execution" 
        in a python program, allowing you to "make decisions" and "repeat tasks".

    -> Python has given three types of Control Structures

        -> Conditional Structures / Statements (Decision Making Statements)

        -> Looping Structures / Statements (Iteration Statements)

        -> Branching Structures / Statements (Jump Statements)
    
    -> NOTE: To Work with above Control Structures, "indentation" knowledge is must


    -> Indentation

        -> Indentation in Python is a fundamental and mandatory aspect of its "syntax", 
        used to define the structure and hierarchy of "code blocks". 
        Unlike many other programming languages that use curly braces or keywords 
        to delineate blocks, Python relies entirely on "consistent" indentation.

        -> Using space as indentation (using spaces correctly is important)

        -> Python allows for any consistent number of spaces or 
        a single tab(4 spaces) for indentation, 
        the widely accepted and recommended standard.

            -> At least one space, but recommended is 4 space i.e tab 

    -> Conditional Structures / Statements (Decision Making Statements)

       -> Used to run a "block of code" based on a condition result  

        -> if 
                -> runs a block of code, if the condition is True 

                    -> syntax 
                        if condition:
                            statements 

        -> if else 

                -> runs a block of code, if the condition is True 
                    runs another block of code, if the condition is False 

                    -> syntax 
                        if condition:
                            statements
                        else:
                            statements

        -> NOTE: input() function reads input from the user
        and stores the input as String 

        -> Interpolation 

                -> Interpolation technique replaces, place holders with actual values dynamically

                -> Using "f-strings" (Formatted String Literals)

                    -> we use f with print and {} as place holders 


2nd Mar 2026
=============

    -> The ternary operator in Python, also known as a 
    "conditional expression", provides a concise way(short hand code) 
    to write an if-else statement on a single line.

    -> NOTE: It evaluates a condition and returns one of two values 
    based on whether the condition is True or False.

    -> syntax 
        value_if_true if condition else value_if_false 

    -> elif ladder 

        -> elif is a keyword that stands for "else if" 

            -> It is used in conditional statements to check 
            "multiple conditions" sequentially after an initial if statement
            
            -> Syntax

                if condition:
                    statements
                elif condition:
                    statements
                elif condition:
                    statements
                elif condition:
                    statements
                elif condition:
                    statements
                else:
                    statements


    -> match/case statement

        -> The match/case statement (introduced in Python 3.10) 
        is used for structural "pattern matching"

        -> In other programming languages we call it as switch-case 

    -> syntax 

            match expression:
                case pattern_1:
                    # code block 1    
                case pattern_2:
                    # code block 2
                case pattern_3:
                    # code block 3
                .
                .
                .
                case _:
                    # default code block


-> Nested Conditionals 

    -> Nested conditions in Python involve placing an if or if/else 
    statement inside another if, elif, or else block.

    -> This allows the program to evaluate multiple criteria sequentially, 
    where the inner condition is only checked if the outer condition is true. 


    -> Looping Structures / Statements (Iteration Statements)

        -> Loops in Python are used to "repeatedly" execute a block of code 
        until a specific condition is met or for each item in a sequence.

        -> The two primary types of loops are "for" loops and "while" loops. 

        -> When you want to do a task multiple times, use loops 

        -> "while" loop

            -> while loops execute a block of code repeatedly 
            as long as a specified condition remains True

            -> syntax

                while condition:
                    statements 

            -> NOTE: We use while generally, when we don't know number of
            Iterations/Repetitions in advance


16th Mar 2026
=============

    -> "for" loop 

        -> for loop is used to iterate over a sequence
        (like a list, tuple, string, range etc)

        -> NOTE: for loop executes a block of code,
        once for each item in the sequence.

        -> syntax 

            for variable_name in sequence:
                statements 

        -> NOTE: We use for generally, when we know number of
        Iterations/Repetitions in advance

    -> range()

        -> The range() function in Python generates an immutable sequence of numbers. 
        It is commonly used for iterating a specific number of times in for loops. 

        -> range(start, stop, step): This form generates a sequence of numbers starting 
        from start (inclusive) up to, but not including, the stop value, 
        with an increment of step. The step can be positive or negative.

                range(start, stop, step)
                    -> start default is 0 (included) (inclusive)
                    -> stop at this value (excluded) (exclusive)
                    -> step is increment (default step is 1)

    -> Nested Loops 

        -> Loops inside Loops 


    -> Branching Structures / Statements (Jump Statements)

        -> Used within loops, these statements also control the flow of execution, 
        allowing you to exit a loop early (break) or skip the current iteration (continue). 

            -> break : help you exit the loops 

            -> continue : help you skip the current iteration 

            -> pass : acts as a placeholder, does nothing


17th Mar 2026
=============

    -> Strings 

        -> Strings are one of the "Most Widely Used Datatype" in any programming language 

        -> https://www.instagram.com/

        -> https://www.instagram.com/accounts/emailsignup/?next=

        -> NOTE: Most of the inputs we process from web pages of an Application are Strings 

    -> In Python, a String is "Sequence Of Characters", used to represent Text 

    -> Strings are widely used for "handling and manipulating textual" information.

    -> Syntax For Representing Strings 

        -> Strings can be enclosed in '' or " " or ''' ''' or """ """

            -> Single Line Strings, we use '' or "" 

            -> Multi Line Strings, we use ''' ''' or """ """

    -> Rules When Defining Strings 

        -> When you use single quote in a string, enclose them in double quotes 
        -> When you use double quote in a string, enclose them in single quotes 
        -> When you use both single quote and double quote in a string,
        enclose them in triple quotes 

    -> Access Strings 

        -> Indexing 

            -> String indexing in Python allows access to "individual characters within a string" 
            using their "position", known as the "index".

            -> Python uses zero-based indexing, meaning the first character is at index 0, 
            the second at index 1, and so on.

                -> syntax: print(string[index])

            -> Positive Indexing:
            Starts from the beginning of the string.
            The index of the first character is 0.

            -> Negative Indexing:
            Starts from the end of the string.
            The index of the last character is -1.

            -> text = "python"

             0.  1.  2.   3.  4.  5.   (positive indexing) 
             p   y   t    h   o   n 
            -6  -5  -4  -3  -2  -1   (negative indexing)

        -> NOTE: Accessing Index Out Of Range will cause Error 

    
    -> Slicing 

        -> String slicing in Python is a method for "extracting a portion" (substring) 
        from a larger string. It utilizes the slice operator [] with specific 
        indices to define the desired segment.

    -> syntax 

        string[start:end:step]

            -> start (optional): The index where the slice begins (inclusive). 
            If omitted, it defaults to 0 (the beginning of the string).
            
            -> end (optional): The index where the slice ends (exclusive). 
            The character at this index is not included. 
            If omitted, it defaults to the length of the string (the end of the string).
            
            -> step (optional): The increment between each index during slicing. 
            If omitted, it defaults to 1. 
            A negative step value can be used to slice in reverse.

                -> Earlier we used range()

                    range(start,stop,step)


    -> String Concatenation 

        -> Joining the strings using + operator

    -> Formatted String Literals (f-strings)

        -> Using Interpolation {} with f 

    -> String Repetition

        -> Repeat Strings Using "*" Operator 

    -> String Immutability 

        -> Strings are Immutable in nature 
        (Immutable = Cannot be Changed / Modified)


18th Mar 2026
=============

    -> String Methods 

        -> String methods in Python are built-in methods that operate on 
        string objects, allowing for various manipulations, transformations, and checks. 

        -> https://docs.python.org/3/library/stdtypes.html#string-methods

        -> It is important to note that strings in Python are immutable, 
        meaning these methods do not modify the original string but 
        instead return a new string with the applied changes.


19th Mar 2026
===============

    -> Using Strings & String Methods From Application Point Of View 

20th Mar 2026
===============

        -> Simple Data Types 

        -> Can Hold "One Value" in a single variable 

            -> data = 10
            -> data = 10.5
            -> data = "hello" 

    -> Complex Data Types 

        -> Can Hold "Multiples Value" in a single variable 

            -> data = 10,20,30,40,50
            -> data = 10.5,3.5.4.5
            -> data = "hello","hi","hey" 

    -> Python provides following classes to work with complex data types 

        -> List
        -> Tuple 
        -> Dictionary
        -> Set 

    
    
    -> List 

        -> List is a data structure which allows you to store multiple values
        in a single variable, each value is separated with , 

        -> Lists are represented using [ ] or using "list" class

    -> Accessing Data In Lists 

        -> Like in Strings, we have index concept in lists too 

        -> Index starts from Zero 

        -> Supports Both Positive and Negative Index 

        -> Slicing is same as Strings 

        -> Loops we can Apply 

        -> Operators we can Apply 

        -> Conditionals we can Apply 

        -> Duplicates Allowed 

        -> Insertion Order Is Preserved 

    -> List Characteristics 

        -> Mutable: Lists are Mutable in nature 

        -> Order: Lists are Order(Order Preserved)

        -> Duplicates: Lists Allow Duplicates 

        -> NOTE: 
        
            -> Requirement: Store Employee Details like AADHAR & PAN

                -> Unique AADHAR ID's only: Lists doesn't satisfy this requirement 
                -> Modify PAN ID 


23rd Mar 2026
===============

    -> Tuples 

        -> Tuple is a data structure which allows you to store multiple values
        in a single variable, each value is separated with , 

        -> Tuples are represented using ( ) or using "tuple" class

    -> Accessing Data In Tuples 

        -> Like in Strings, we have index concept in Tuples too 

        -> Index starts from Zero 

        -> Supports Both Positive and Negative Index 

        -> Slicing is same as Strings 

        -> Loops we can Apply 

        -> Operators we can Apply 

        -> Conditionals we can Apply 

        -> Duplicates Allowed 

        -> Insertion Order Is Preserved 

    -> NOTE: Tuples are "IMMUTABLE" (Cannot be Changed i.e FIXED DATA)  

    -> NOTE: Tuples are "READ ONLY LISTS"   

    -> Tuples Characteristics 

        -> Immutable: Tuples are Immutable in nature(Cannot be Changed) 

        -> Order: Tuples are Order(Order Preserved)

        -> Duplicates: Tuples Allow Duplicates     


    
    -> Dictionaries 

        -> Dictionary is a data structure which allows you to store multiple "key values"
        in a single variable, each value is separated with ,

        -> Dictionaries are represented using { } or using "dict" class

        -> key:value (pair/item)

            -> {"id":101,"name":"ravi","gpa":9.5} 

    -> Accessing Data In Dictionaries 

        -> NOTE: Like in Strings, we DON'T have index concept in Dictionaries

            -> Dictionaries don't support Indexing

            -> Access individual elements using "key" 

        -> Slicing is not supported in Dictionaries

        -> Loops we can Apply 

        -> Operators we can Apply 

        -> Conditionals we can Apply 

        -> Duplicates Allowed In Dictionaries With Some Conditions 

            -> Values can be Duplicated 

            -> Keys Should be Unique 

                -> If we add duplicates key, latest will override old key 
            
        -> NOTE: Keys should be Immutable Objects Only

            -> 95% above times the keys will be Strings  

        -> NOTE: Values can be Any Object 

        -> Insertion Order Is Preserved 

        -> NOTE: Dictionaries look like json data 

            -> https://media.licdn.com/dms/image/v2/D4D12AQGwOUMYbhUu-A/article-cover_image-shrink_720_1280/article-cover_image-shrink_720_1280/0/1682148646113?e=2147483647&v=beta&t=qeCSY5Ktzx2jkeq7suYaSBV_-OS_18P-yuabrIhNWcU

    -> Dictionary Characteristics 

        -> Mutable: Dictionaries are mutable 

            -> Immutable: Dictionaries Keys Should Be Immutable

        -> Order: Dictionaries are Order(Order Preserved)

        -> Duplicates: Dictionaries Allow Duplicates For Values Only, 
        Duplicated Keys allowed, but overridden 


24th Mar 2026
===============

    -> Sets 

        -> Set is a data structure which allows you to store multiple values
        in a single variable, each value is separated with , 

        -> Sets are represented using { } or using "set" class

    -> Accessing Data In Sets 

        -> Like in Strings, we have DON'T index concept in sets 
        
        -> No Indexing

        -> No Slicing 

        -> Loops we can Apply 

        -> Operators we can Apply 

        -> Conditionals we can Apply 

        -> Duplicates Not Allowed i.e Removes Duplicates By default

        -> Insertion Order Is Not Preserved 

        -> To Easily Remember about Sets (UUU)

            -> U - Unique 
            -> U - Unordered
            -> U - Unindex 

    -> Set Methods / Operations 

    -> NOTE: Sets are Mutable 

    -> Set Characteristics 

        -> Mutable: Sets are Mutable in nature(Can be Changed) 

        -> UnOrdered: Sets are UnOrdered(Order Not Preserved)

        -> Duplicates: Sets Doesn't Allow Duplicates 

    
    -> Frozenset

        -> Frozenset is a data structure which allows you to store multiple values
        in a single variable, each value is separated with , 

        -> Frozenset are represented using or using "Frozenset" class

        -> Accessing Data In Frozenset 

        -> Like in Strings, we DON'T have index concept in Frozenset 

        -> No Indexing 
        
        -> No Slicing 

        -> Loops we can Apply 

        -> Operators we can Apply 

        -> Conditionals we can Apply 

        -> Duplicates Not Allowed i.e Removes Duplicates By Default

        -> Insertion Order Is Not Preserved 

        -> NOTE: To Easily Remember Sets use (UUU)

            -> U - Unique 
            -> U - Unordered 
            -> U - Unindex 


        -> Frozenset Methods / Operations 

        -> NOTE: Frozenset are Immutable 

        -> Frozenset Characteristics 

            -> Immutable: Frozenset are Immutable in nature(cannot be Changed) 

            -> UnOrdered: Sets are UnOrdered(Order Not Preserved)

            -> Duplicates: Sets Doesn't Allow Duplicates 


    -> How / When To Use Above Data Structures In Applications ?

        -> Lists            (When To Use)
        -> Tuples           (When To Use)
        -> Dictionaries     (When To Use)
        -> Sets             (When To Use)


25th Mar 2026
===============

    -> CRUD Applications 

        -> CRUD stands for Create, Read, Update, and Delete, representing the four fundamental,
        persistent operations for managing data within databases and software applications. 
        
        -> These operations allow users to add new data, view existing data, modify data, and 
        remove data, forming the backbone of most web applications and user interfaces. 

        -> We CRUD for applications that require persistent storage, such as content management 
        systems, e-commerce sites, and customer relationship management (CRM) tools. 
        Examples include online forms, user profile pages, and dashboards for managing records.

            -> C - CREATE
            -> R - READ
            -> U - UPDATE
            -> D - DELETE 

        -> https://drive.google.com/file/d/1Yabj4nFUx_1g6eAw3EsEsopEWxivTK2Z/view?usp=sharing

        -> https://drive.google.com/file/d/1gpWYycwbeRWrmShF5Hc1V86KcIVYJCfE/view?usp=sharing


26th Mar 2026
===============

    -> CRUD Application Implementation

27th Mar 2026
===============

    -> Functional Programming 

        -> In computer science, functional programming is a programming paradigm 
        where programs are constructed by applying and composing "functions"

    -> Functions 

        -> In Python, a function is a named, "reusable block of code" that performs 
        a specific task, takes input (arguments), processes data, 
        and can return an output.

    -> Why Functions 

        -> Functions can break down complex problems into smaller and
        simpler manageable sub tasks for promoting code modularity. 

        -> Once defined a function, it can be "called multiple times", so that 
        we can achieve code reusability.

    -> Functions are defined using "def" keyword, followed by function name,
        parenthesis(which may contain parameters), and a colon:
        the code within the function is indented 

        -> syntax

            def function_name():
                statements

            def function_name(parameters):
                statements

        -> NOTE: calling function is must

            function_name()

        -> Python functions can be defined to accept "parameters" 
        which can act as placeholders for values that will be 
        passed into function when called. 

        -> Values passed to the function we call them as "arguments"

        -> We use parameters to make functions dynamic in nature 


    -> Based On "arguments" passed to functions, we got five types 

        -> Positional Arguments 

        -> Keywords Arguments 

        -> Default Arguments 

        -> Arbitrary Positional Arguments

        -> Arbitrary Keywords Arguments

    
    -> Positional Arguments 

        -> In Python, positional arguments are values passed to a function during 
        its call, where the assignment of these values to the function's parameters 
        is determined solely by their "order" (position) in the argument list. 
        
        -> The first argument in the function call is assigned to the first parameter 
        in the function definition, the second to the second, and so on. 

        -> Order Matters: If the order is incorrect, the function will receive the wrong values 
        for its intended parameters, potentially leading to errors or unexpected behavior.

    
    -> Keywords Arguments

        -> In Python, keyword arguments are a way to pass values to a function by explicitly 
        "naming the parameters" they correspond to, rather than relying on their position 
        in the argument list. 
        
        -> This offers several advantages, primarily in terms of code readability and flexibility.


    -> Default Arguments 

        -> In Python, default arguments allow you to define a function parameter 
        with a "pre-assigned" value. 
        
        -> If a caller does not provide an argument for that parameter during a function call, 
        the default value is automatically used. 
        If an argument is provided, it overrides the default. 

        -> Placement Requirement: Default arguments must always be placed after 
            any non-default (positional) arguments in the function definition. 
            Attempting to place a non-default argument after a 
            default argument will result in a SyntaxError.       

30th Mar 2026
===============

    -> Arbitrary Positional Arguments

        -> In Python, arbitrary positional arguments allow a function to accept 
        a variable/unknown number of non-keyword arguments. 
        This is achieved by placing an asterisk (*) before a parameter name 
        in the function definition. 
            
        -> This parameter will then collect all the extra positional arguments passed 
        to the function into a single tuple.


    -> Arbitrary Keywords Arguments

        -> In Python, arbitrary keyword arguments allow a function to accept 
        a variable/unknown number of keyword arguments. 
        This is achieved by placing an double asterisk (**) before a parameter name 
        in the function definition. 

        -> This parameter will be collected into a dictionary, where the names will be keys and 
        values will be dictionary values 



    -> return 

        -> In Python, the "return" keyword is used within a function to send a value 
        back to the part of the code that called the function.

            -> Task -> O/P -> I/P Another Task 

            -> Task -> return O/P -> I/P Another Task 

        -> NOTE: When you don't use a return, by default a function returns "None" 

        -> When working with function composition (function calling another function)
        return will be very helpful.


        -> If you use return, make sure it's the last part of statement to be executed

        -> If you have multiple return statements, first will be considered

        -> If multiple returns are present, and used with conditionals, 
        you can control the flow


31st Mar 2026
===============

    -> Scopes In Functions 

        -> Scope: Inside the code blocks, where you can access variables 
        depends on the scope 

            -> Local Scope 

            -> Global Scope 

        -> Local Scope 

            -> A local variable i.e declared "inside the function"
            -> It's lifetime exists only within the function 
            -> We Cannot use them outside the function
            -> NOTE: Parameters we are passing to the functions, are also local variables


        -> Global Scope 

            -> A Global variable is declared "outside the function" 
            -> It's lifetime exists within the function and also outside the function

            -> As Global Variable can be accessed inside the function and outside as well
            there can be name conflict issue, in this scenario preference is given to 
            local variables 

                -> NOTE: To Access Global Variable In Above Scenario, use globals()

            -> If you want global variables to be changed inside the function, 
            we cannot do that directly 

                -> NOTE: To change Global Variable inside the function, we use global keyword 


    -> Types Of Functions 

        -> Built In Functions

        -> User Defined Functions 

        -> Lambda Functions 

        -> Higher Order Functions 

    
    -> Built In Functions

        -> These are pre defined functions, already part of Standard Python Library 

        -> No need to create these functions again using def keyword 

        -> Just call these functions using name 

        -> Examples Include 

            -> print()
            -> id()
            -> type()
            -> dir()
            -> input()
            -> etc 

    
    -> User Defined Functions 

        -> These are user defined functions, which are created by Developers 
        using def keyword 

        -> These functions are created based on Customers Requirement 

        -> Examples Include 

            -> add()
            -> employee_info()
            -> math_ops()
            -> etc 

        
            